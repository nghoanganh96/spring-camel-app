<routes xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://camel.apache.org/schema/spring"
        xsi:schemaLocation="http://camel.apache.org/schema/spring https://camel.apache.org/schema/spring/camel-spring.xsd">

    <route id="SpringJpaSaveCardInfo">
        <from uri="activemq-demo:queue:anhnh.spring.save.request?asyncConsumer=true" />
        <log message="Request Body of SpringJpaSaveCardInfo: ${body}" />
        <bean ref="cardInfoService" method="saveCardInfo"/>
        <log message="Request Body of saveCardInfo: ${body}" />
        <to uri="activemq-demo:queue:anhnh.spring.save.response" />
    </route>

    <route id="SpringJpaGetOneCardInfo">
        <from uri="activemq-demo:queue:anhnh.spring.getone.request?asyncConsumer=true" />
        <log message="Request Body of SpringJpaGetOneCardInfo: ${body}" />
        <bean ref="cardInfoService" method="getOneCardInfo"/>
        <log message="Request Body of getOneCardInfo: ${body}" />
        <to uri="activemq-demo:queue:anhnh.spring.getone.response" />
    </route>

    <route id="SpringJpaAddCardInfoByUser">
        <from uri="activemq-demo:queue:anhnh.spring.addcardbyuser.request?asyncConsumer=true" />
        <log message="Request Body of RouteId SpringJpaAddCardInfoByUser: ${body}" />
        <bean ref="cardInfoService" method="addCardInfoByUser"/>
        <log message="Request Body of Method addCardInfoByUser: ${body}" />
        <to uri="activemq-demo:queue:anhnh.spring.addcardbyuser.response" />
    </route>

</routes>